# Migration `watch-20191110231844`

This migration has been generated by philippwalterJS at 11/10/2019, 11:18:44 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "basstracks"."Artist" (
  "id" text NOT NULL  ,
  "name" text NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

ALTER TABLE "basstracks"."Track" DROP COLUMN "author",
DROP COLUMN "content",
ADD COLUMN "artist" text   REFERENCES "basstracks"."Artist"("id") ON DELETE SET NULL,
ADD COLUMN "difficulty" text NOT NULL DEFAULT 'BEGINNER' ,
ADD COLUMN "genre" text   ,
ADD COLUMN "notes" text   ,
ADD COLUMN "user" text   REFERENCES "basstracks"."User"("id") ON DELETE SET NULL;

ALTER TABLE "basstracks"."User" ADD COLUMN "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
ADD COLUMN "role" text NOT NULL DEFAULT 'USER' ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20191110222931..watch-20191110231844
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = "postgresql://prisma:prisma@localhost:5432/postgres?schema=basstracks"
 }
 model User {
   id        String   @default(cuid()) @id
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191110231844)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191110231844'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
